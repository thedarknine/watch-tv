{% extends 'base.html.twig' %}

{% block title %}Hello SyncController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Synchronisation</h1>

    <ul>
        <li>Chaînes ✅ <code>plop</code></li>
    </ul>

    Liste des chaînes souhaitées
    <ul>
        {% for element in data.channels %}
            <li style="margin-top:20px;"><img src={{element.logo}} alt="{{element.id}}" width="20" /> {{element.name}}</li>
            {#% for program in element.programs %#}
                {# <li>{{program.title}}</li> #}
            {#% endfor %#}
        {% endfor %}
    </ul>
</div>
{% endblock %}